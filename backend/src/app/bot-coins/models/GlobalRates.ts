import {SymbolValue} from "./SymbolValue";

export class GlobalRates {
    [index: string]: SymbolValue;

    // constructor(data: any) {
    //     Object.keys(data).forEach((key) => {
    //         this[key] = new SymbolValue(data);
    //     });
    // }
}